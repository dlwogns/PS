#include <iostream>
#include <algorithm>
using namespace std;
int T;
int main() {
    ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    cin>>T;
    while(T--) {
        int N, arr[200001], ans=0; cin>>N;
        // 1 1 13 -> 1
        // 완탐 불가. -> 2^N
        // 
        // T -> 10^4, N -> 2*10^5
        
    }
}